<form method="post" action="{{ path('app_ancien_etudiant_delete', {'id': ancien_etudiant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ancien_etudiant.id) }}">
    <button class="btn">Delete</button>
</form>
